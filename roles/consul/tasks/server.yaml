---
- name: "consul | render server config"
  ansible.builtin.template:
    src: "server.hcl.j2"
    dest: "/etc/consul.d/server.hcl"
    owner: "consul"
    group: "consul"
    mode: '0644'
  notify:
    - "restart consul"
