# this file is managed by ansible, resistance is futile
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow local connections
local   all             all                                     peer

# Allow replication connections from the primary server
host    replication     all             {{ postgres.replication_network }}     md5

# Allow connections from trusted clients (adjust based on your network)
host    all             all             {{ postgres.trusted_network }}         md5
